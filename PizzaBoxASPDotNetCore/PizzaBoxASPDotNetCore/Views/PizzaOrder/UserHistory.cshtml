@model IEnumerable<PizzaBoxWeb.Models.PizzaOrderViewModel>

@{
    ViewData["Title"] = "UserHistory";
    TempData.Keep("Login"); 

}

<h4 class="text-center">@TempData["Login"].ToString() :  Your Past Orders </h4>

<table class="table">
    <thead>
        <tr>
            <th>
                Order Number
            </th>
            <th>
               Storename
            </th>
            <th>
               Order Price
            </th>
            <th>
               Order Date
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Orderid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Storename)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderDate)
            </td>
        </tr>
}
    </tbody>
</table>

<div>
    <a class="btn btn-primary btn-lg" asp-controller="Home" asp-action="LoggedIn" role="button"> Back to Menu</a>
</div>